# ===================================================================
# LinkBox Backend Environment Configuration
# ===================================================================
# Copy this file to .env and update values based on your environment
#
# For LOCAL DEVELOPMENT (with docker-compose):
#   - Use the local database settings below
#   - Start with: docker-compose up -d db
#
# For AWS PRODUCTION:
#   - Database credentials are managed via SSM Parameter Store
#   - S3 bucket is created by CloudFormation
#   - These values are injected by deployment scripts
# ===================================================================

# Environment (dev, staging, production)
ENVIRONMENT=dev

# AWS Configuration
AWS_REGION=us-east-1

# S3 Bucket for file uploads
# Local dev: Use a test bucket or localstack
# Production: Managed by CloudFormation (linkbox-uploads)
S3_BUCKET_NAME=linkbox-dev-bucket

# Database Connection
# Local dev: Use docker-compose postgres (see docker-compose.yml)
# Production: Automatically constructed from SSM Parameter Store
DATABASE_URL=postgresql://linkbox_user:linkbox_password@localhost:5432/linkbox_dev

# File Upload Configuration
PRESIGNED_EXPIRY_SECONDS=3600        # 1 hour
MAX_UPLOAD_BYTES=10485760            # 10 MB

# CloudFront Domain (optional, for download URLs)
# Leave empty for local dev
# Production: Get from CloudFormation outputs
CLOUDFRONT_DOWNLOAD_DOMAIN=

# ===================================================================
# PRODUCTION VALUES (automatically set by deployment scripts)
# ===================================================================
# In production (AWS EC2), these values are set by:
# - SSM Parameter Store: /linkbox/db-endpoint, /linkbox/db-username, etc.
# - CloudFormation: S3 bucket name, CloudFront domain
# - Deployment scripts: scripts/start_application.sh
#
# You don't need to manually set these for production deployments.
# ===================================================================
