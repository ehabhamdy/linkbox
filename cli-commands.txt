aws cloudformation create-stack \
--stack-name udacity-workflow-lesson \
--template-body file://network.yml \
--parameters file://network-parameter.json \
--region us-east-1

aws cloudformation create-stack udacity-workflow-lesson \
--template-body file://network.yml \
--region us-east-1

aws cloudformation create-stack udacity-workflow-lesson \
--template-body file://network.yml \
--region us-east-1


aws cloudformation create-stack \
--stack-name udacity-workflow-lesson \
--template-body file://network.yml \
--region us-east-1

aws cloudformation update-stack \
    --stack-name udacity-workflow-lesson \
    --template-body file://network.yml \
    --parameters file://network-parameter.json \
    --region us-east-1